//Define macro shortcuts

#pragma once
#include "macros.h"
#define sayl(m) std::cout << m << std::endl //Define a macro shortcut for outputing a line to console because why not
#define say(m) std::cout << m